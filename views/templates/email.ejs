<!DOCTYPE html>
<html lang="en-US">
  <head>
    <meta content="text/html; charset=utf-8" http-equiv="Content-Type" />
    <title>Reset Password Email Template</title>
    <meta name="description" content="Reset Password Email Template." />
    <style type="text/css">
      a:hover {
        text-decoration: underline !important;
      }

      body {
        margin: 0px;
        background-color: #f2f3f8;
        font-family: "Open Sans", sans-serif;
        width: 100%;
      }

      table {
        background-color: #f2f3f8;
        max-width: 670px;
        margin: 0 auto;
      }

      h1 {
        color: #1e1e2d;
        font-weight: 500;
        margin: 0;
        font-size: 32px;
        font-family: "Rubik", sans-serif;
      }

      p {
        color: #455056;
        font-size: 15px;
        line-height: 24px;
        margin: 0;
      }

      a {
        background: #dae4fb;
        text-decoration: none !important;
        font-weight: 500;
        margin-top: 35px;
        color: #2535ab;
        text-transform: uppercase;
        font-size: 14px;
        padding: 10px 24px;
        display: inline-block;
        border-radius: 50px;
      }

      p.footer {
        font-size: 14px;
        color: #2535ab;
        line-height: 18px;
        margin: 0 0 0;
      }

      .content-table {
        max-width: 670px;
        background: #fff;
        border-radius: 3px;
        text-align: center;
        -webkit-box-shadow: 0 6px 18px 0 #2535ab;
        -moz-box-shadow: 0 6px 18px 0 #2535ab;
        box-shadow: 0 6px 18px 0 #2535ab;
      }
    </style>
  </head>

<body>
  <!-- Main table to center email content on the page -->
  <table>
    <tr>
      <td>
        <!-- Nested table for the email content -->
        <table width="100%">
          <!-- Additional table rows for styling and content... -->
          <tr>
            <!-- Logo of the company -->
            <td style="text-align: center">
              <img width="40%" src="https://i.postimg.cc/c4xcbmjB/Techommeng-Logo.png" title="logo" alt="logo" />
            </td>
          </tr>
          <!-- Additional table rows for styling and content... -->
          <tr>
            <td>
              <!-- Content table for the main email text -->
              <table class="content-table" width="95%" cellpadding="0" cellspacing="0">
                <!-- Additional table rows for styling and content... -->
                <tr>
                  <td style="padding: 0 35px">
                    <!-- Title of the email -->
                    <h1>You have requested to reset your password</h1>
                    <!-- Content of the email -->
                    <p>
                      To reset your password, click the following link and
                      follow the instructions. If you did not request a new
                      password, please let us know immediately by replying to
                      this email.
                    </p>
                    <!-- Reset password link, using an ejs template variable -->
                    <a href="<%= resetLink %>">Reset Password</a>
                  </td>
                </tr>
                <!-- Additional table rows for styling... -->
              </table>
            </td>
          </tr>
          <!-- Additional table rows for styling and content... -->
          <tr>
            <!-- Footer of the email -->
            <td style="text-align: center">
              <p class="footer">&copy; <strong>2023 Techommend</strong></p>
            </td>
          </tr>
          <!-- Additional table rows for styling... -->
        </table>
      </td>
    </tr>
  </table>
</body>
</html>
